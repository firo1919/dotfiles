# File system
if command -v eza >/dev/null 2>&1; then
  alias ls='eza -lh --group-directories-first --icons=auto'
  alias lsa='ls -a'
  alias lt='eza --tree --level=2 --long --icons --git'
  alias lta='lt -a'
fi


if command -v zoxide >/dev/null 2>&1; then
  alias cd="zd"
  zd() {
    if [ $# -eq 0 ]; then
      builtin cd ~ && return
    elif [ -d "$1" ]; then
      builtin cd "$1"
    else
      z "$@" && printf "\U000F17A9 " && pwd || echo "Error: Directory not found"
    fi
  }
fi

open() {
  xdg-open "$@" >/dev/null 2>&1 &
}

# Directories
alias ..='cd ..'
alias ...='cd ../..'
alias ....='cd ../../..'

# Tools
alias d='docker'
alias rm='trash'
alias c='clear'
alias rc='rclone rcd --rc-web-gui'
alias cat='bat'
alias spring='mvn spring-boot:run -Dspring-boot.run.profiles=dev'
alias gen-secret='openssl rand -hex 32'
alias y='yazi'
alias dl='lazydocker'
alias adb='HOME="$XDG_DATA_HOME"/android adb'
alias pipinstall='pip install -r requirements.txt'
alias zed='zeditor'
alias list-backups='restic snapshots -r rclone:EncryptedGoogleDrive:Backups --password-command "rbw get restic-pass"'

# Git
alias g='git'
alias ga='git add'
alias gcm='git commit -m'
alias gcam='git commit -a -m'
alias gcad='git commit -a --amend'
alias gs='git status --short'
alias gcl='git clone'
alias gi='git init'
alias gp='git pull'
alias gpu='git push'
alias gl='git log'

# Fzf
alias pf="paru -Slq | fzf --multi --preview 'paru -Si {1}' | xargs -ro paru -S"
alias ff="fzf --preview 'bat --style=numbers --color=always {}'"
